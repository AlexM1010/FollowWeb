# FollowWeb Environment Variables Template
# Copy this file to .env and fill in your values
# NEVER commit the .env file to git!

# Test Data Generation Salt (required for secure test data generation)
# Generate a new salt with: python -c "import secrets; print(secrets.token_hex(32))"
# This salt makes username hashing irreversible - keep it secret!
TEST_DATA_SALT=your_64_character_hex_salt_here

# Freesound API Credentials (optional - only needed for Freesound data collection)
FREESOUND_API_KEY=your_freesound_api_key_here

# GitHub Token (optional - only needed for checkpoint backup to private repo)
GITHUB_TOKEN=your_github_personal_access_token_here
