# Minimal dependencies for format checking and linting only
# Used in jobs that only need code quality tools

ruff>=0.1.0
mypy>=1.0.0
types-python-dateutil
types-PyYAML
types-decorator
types-requests
types-setuptools